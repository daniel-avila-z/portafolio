/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
  // output: 'export'
}

module.exports = nextConfig

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   output: "export",
//   // more
// };

// module.exports = nextConfig;
